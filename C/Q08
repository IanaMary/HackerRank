Sum of Digits of a Five Digit Number
Soma dos dígitos de um número de cinco dígitos
-----------------------------------------------

int main() {
	
    int n;
    scanf("%d", &n);
    int soma = 0;
    while(n > 0) {
        soma = soma + ((n - (n/10)*10));
        n = n/10; 
    }
    printf("%d", soma);
    //Complete the code to calculate the sum of the five digits on n.
    return 0;
}
