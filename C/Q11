Sum and Difference of Two Numbers
Soma e diferença de dois números
----------------------------------

int main()
{
	int a[2];
    double b[2];

    for(int i = 0; i < 2; i++) {
        scanf("%d", &a[i]);
    }

     for(int i = 0; i < 2; i++) {
        scanf("%lf", &b[i]);
    }

    printf("%d %d \n", (a[0] + a[1]), (a[0] - a[1]));
    printf("%.1lf %.1lf \n", (b[0] + b[1]), (b[0] - b[1]));

    return 0;
}

